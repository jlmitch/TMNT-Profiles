import styled from 'styled-components';

/**
 * The header used on like every single view
 */
const NavWrap = styled.header`
  background-color: #8CC53F;
  display: flex;
  justify-content: flex-end;
  height: 60px;
`;

export default NavWrap;